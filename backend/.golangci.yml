run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - errorlint
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - gosec
    - misspell
    - unconvert
    - unparam
    - gofmt
  disable:
    - depguard

linters-settings:
  govet:
    shadow: true
  gocritic:
    enabled-tags:
      - diagnostic
      - performance
    disabled-checks:
      - hugeParam
  gosec:
    excludes: []
  misspell:
    locale: US
  errorlint:
    errorf: true
    errorf-multi: true
    asserts: true
    comparison: true

issues:
  exclude-dirs:
    - vendor
    - docs
    - web
    - sdk
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - errcheck
    - path: test/
      linters:
        - gosec
        - errcheck
    # Exclude existing code from errorlint to avoid a mass fix.
    # Remove entries progressively as packages are updated.
    - path: pkg/
      linters:
        - errorlint
    - path: internal/
      linters:
        - errorlint
  max-issues-per-linter: 50
  max-same-issues: 5
