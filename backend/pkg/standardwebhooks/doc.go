// Package standardwebhooks implements the Standard Webhooks specification
// (webhook-id, webhook-timestamp, webhook-signature headers, envelope format,
// retry semantics) and CNCF CloudEvents v1.0 support (structured + binary
// content modes) with automatic format detection and bidirectional conversion.
package standardwebhooks
