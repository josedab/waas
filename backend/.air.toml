# Air configuration for hot-reload during development
# Install: go install github.com/air-verse/air@latest
# Usage: make dev

root = "."
tmp_dir = "tmp"

[build]
  # Build command — defaults to api-service; override with AIR_BUILD_CMD env var
  cmd = "go build -o ./tmp/api-service ./cmd/api-service"
  bin = "./tmp/api-service"
  delay = 1000
  exclude_dir = ["tmp", "bin", "vendor", "node_modules", "web", "docs", "deploy", "k8s", "test", "migrations"]
  exclude_file = []
  exclude_regex = ["_test\\.go$"]
  exclude_unchanged = false
  follow_symlink = false
  include_dir = []
  include_ext = ["go", "toml", "yaml", "yml"]
  include_file = []
  kill_delay = "0s"
  log = "build-errors.log"
  poll = false
  poll_interval = 0
  rerun = false
  rerun_delay = 500
  send_interrupt = true
  stop_on_error = true

[color]
  app = ""
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  main_only = false
  time = false

[misc]
  clean_on_exit = true

[screen]
  clear_on_rebuild = false
  keep_scroll = true
