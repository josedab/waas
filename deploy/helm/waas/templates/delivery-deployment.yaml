apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "waas.fullname" . }}-delivery
  namespace: {{ include "waas.namespace" . }}
  labels:
    {{- include "waas.labels" . | nindent 4 }}
    app.kubernetes.io/component: delivery
spec:
  {{- if not .Values.delivery.autoscaling.enabled }}
  replicas: {{ .Values.delivery.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "waas.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: delivery
  template:
    metadata:
      labels:
        {{- include "waas.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: delivery
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.delivery.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: {{ include "waas.serviceAccountName" . }}
      securityContext:
        {{- include "waas.podSecurityContext" . | nindent 8 }}
      containers:
      - name: delivery-engine
        image: "{{ .Values.delivery.image }}:{{ .Values.global.imageTag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.delivery.port }}
          name: metrics
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ .Values.postgresql.existingSecret | default (printf "%s-secrets" (include "waas.fullname" .)) }}
              key: DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: {{ .Values.redis.existingSecret | default (printf "%s-secrets" (include "waas.fullname" .)) }}
              key: REDIS_URL
        - name: WEBHOOK_SIGNING_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ .Values.postgresql.existingSecret | default (printf "%s-secrets" (include "waas.fullname" .)) }}
              key: WEBHOOK_SIGNING_SECRET
        envFrom:
        - configMapRef:
            name: {{ include "waas.fullname" . }}-config
        resources:
          {{- toYaml .Values.delivery.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.delivery.port }}
          initialDelaySeconds: {{ .Values.delivery.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.delivery.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.delivery.livenessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.delivery.livenessProbe.failureThreshold }}
        readinessProbe:
          httpGet:
            path: /ready
            port: {{ .Values.delivery.port }}
          initialDelaySeconds: {{ .Values.delivery.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.delivery.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.delivery.readinessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.delivery.readinessProbe.failureThreshold }}
        securityContext:
          {{- include "waas.containerSecurityContext" . | nindent 10 }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
      terminationGracePeriodSeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "waas.fullname" . }}-delivery
  namespace: {{ include "waas.namespace" . }}
  labels:
    {{- include "waas.labels" . | nindent 4 }}
    app.kubernetes.io/component: delivery
spec:
  selector:
    {{- include "waas.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: delivery
  ports:
  - port: {{ .Values.delivery.port }}
    targetPort: {{ .Values.delivery.port }}
    name: metrics
  type: ClusterIP
